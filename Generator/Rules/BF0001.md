# BF0001: Invalid value parameter

## Cause
The `imagePath` or `sourcePath` provided to the `GenerateBrainfuckMethodAttribute` is null or whitespace.

## Solution
Ensure a valid, non-empty source path or inline source is provided to the attribute.
