# BF0011: Method must be partial

## Cause
The method decorated with `GenerateBrainfuckMethodAttribute` is not declared with the `partial` modifier.

## Solution
Add the `partial` modifier to the method definition.
