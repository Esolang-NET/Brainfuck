# BF0002: Unsupported return type

## Cause
The method's return type is not supported by the Brainfuck source generator.

## Solution
Change the return type to one of the supported types (e.g., `void`, `int`, `string`, `Task`, `ValueTask`, `IEnumerable<byte>`).
